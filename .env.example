# Docker Compose (локальная разработка / демо)
COMPOSE_PROJECT_NAME=ktk

# Postgres (сервис postgres в compose; пока только заготовка)
POSTGRES_USER=ktk
POSTGRES_PASSWORD=ktk
POSTGRES_DB=ktk
DATABASE_URL=postgresql://ktk:ktk@postgres:5432/ktk
